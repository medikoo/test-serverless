service: test-issue-11748

provider:
  name: aws
  runtime: nodejs14.x
  region: us-east-1
  versionFunctions: false
  apiGateway:
    restApiId: pkhriceyzl # TODO: Replace with accessible REST API id
    restApiRootResourceId: 1xufji4tak # TODO: Replace root resource id for above listed Rest API

functions:
  myFunction:
    handler: index.handler
    events:
      - http:
          path: /release
          method: GET
          private: true
          request:
            parameters:
              querystrings:
                AccountGuid: true
                TelephoneNumber: true
