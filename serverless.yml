service: test-python-lambda

provider:
  name: aws
  runtime: python3.8
  region: us-east-1
  stage: dev
  versionFunctions: false

functions:
  function:
    handler: handler.hello
    maximumRetryAttempts: 0
    provisionedConcurrency: 1
