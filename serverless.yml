service: test-api-gw-shared

provider:
  name: aws
  runtime: nodejs10.x
  region: us-east-1
  versionFunctions: false
  apiGateway:
    restApiId: pkhriceyzl
    restApiRootResourceId: 1xufji4tak

package:
  individually: true

functions:
  myFunction:
    handler: index.handler
    events:
      - http:
          path: bar
          method: GET
