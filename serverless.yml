service: test-lambda-with-layer

provider:
  name: aws
  runtime: nodejs14.x
  region: us-east-1
  stage: dev
  versionFunctions: false

package:
  individually: true

functions:
  function:
    handler: index.handler
    layers:
      - { Ref: Layer1LambdaLayer }

layers:
  layer1:
    path: layer
