service: test-lambda-individually

provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1
  stage: dev
  versionFunctions: false

functions:
  function:
    handler: foo/index.handler
    package:
      individually: true
      exclude:
        - "**"
      include:
        - "foo/**"
