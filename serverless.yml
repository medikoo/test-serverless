service: test-regular-lambda

provider:
  name: aws
  runtime: nodejs10.x
  region: us-east-1
  stage: dev
  versionFunctions: false

package:
  individually: true

functions:
  myFunction:
    handler: index.handler

plugins:
  - serverless-plugin-reducer
