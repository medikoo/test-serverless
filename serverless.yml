service: test-7822

provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1
  stage: dev

package:
  individually: true

functions:
  function:
    handler: index.handler

plugins:
  - serverless-plugin-reducer
