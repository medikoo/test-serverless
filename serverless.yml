service: test-issue-plugin-592

org: medikoo
app: medikoo-test

provider:
  name: aws
  runtime: python3.8
  region: us-east-1
  stage: dev
  versionFunctions: false

functions:
  function:
    handler: handler.process
    runtime: python3.7
    events:
      - schedule: rate(1 minute)

plugins:
  - serverless-python-requirements
