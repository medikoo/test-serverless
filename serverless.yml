service: test-dashboard-outputs

tenant: medikoo
app: medikoo-test

outputs:
  my-key: my-value
  test.dotted: foo
  my-availability-zones:
    - us-east-1a
    - us-east-1b

provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1
  stage: dev
  versionFunctions: false

package:
  individually: true

functions:
  function:
    handler: index.handler

plugins:
  - serverless-plugin-reducer
