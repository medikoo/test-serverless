service: issue-7788

provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1
  stage: dev
  versionFunctions: false

package:
  individually: true

functions:
  function:
    handler: index.handler
    provisionedConcurrency: 1
    events:
      - http:
          path: /test
          method: get

plugins:
  - serverless-plugin-reducer
