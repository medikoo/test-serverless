service: ${file(./config.sandbox.yml):servicesPrefixName}-xyz

variablesResolutionMode: 20210326

custom:
  myStage: "sandbox"

provider:
  name: aws
  lambdaHashingVersion: 20201221
  runtime: nodejs14.x

functions:
  function:
    handler: index.handler
