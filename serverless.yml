service: test-package-option

provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1
  stage: dev
  versionFunctions: false

package:
  individually: true

plugins:
  - serverless-plugin-reducer
