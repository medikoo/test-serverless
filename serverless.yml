service: test-lambda-s3-custom

provider:
  name: aws
  runtime: nodejs12.x
  versionFunctions: false

package:
  individually: true

functions:
  function:
    handler: index.handler

plugins:
  - serverless-plugin-reducer
