service: test-vpc

provider:
  name: aws
  runtime: nodejs10.x
  region: us-east-1
  stage: dev
  versionFunctions: false

vpc: true

package:
  individually: true

functions:
  myFunction:
    handler: index.handler

plugins:
  - serverless-plugin-reducer
