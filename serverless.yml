service: test-express-serverless-http

provider:
  name: aws
  runtime: nodejs14.x
  region: us-east-1
  stage: dev
  versionFunctions: false

package:
  individually: true

functions:
  api:
    handler: index.handler
    events:
      - httpApi: "*"
