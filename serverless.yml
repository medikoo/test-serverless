service: test-custom-provider

provider:
  name: custom
  foo: bar
  elo: mar

functions:
  function:
    handler: index.handler
    events:
      - alb:
          test: "fsdf"

plugins:
  - ./provider-plugin
