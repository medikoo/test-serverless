service: test-console

org: medikoo
console: true

provider:
  name: aws
  runtime: nodejs14.x
  region: us-east-1
  stage: dev
  versionFunctions: false

functions:
  success:
    handler: success.handler
  timeout:
    handler: timeout.handler
