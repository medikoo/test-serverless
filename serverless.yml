service: test-dashboard

tenant: medikoo
app: medikoo-test

provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1
  stage: many-params
  versionFunctions: false

package:
  individually: true

functions:
  function:
    handler: index.handler

plugins:
  - serverless-plugin-reducer
