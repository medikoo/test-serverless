service: test-1x

provider:
  name: aws
  runtime: nodejs10.x

package:
  individually: true

functions:
  myFunction:
    handler: index.handler
  myFunction2:
    handler: index.handler

plugins:
  - serverless-plugin-reducer
